'use client';

import GradebookManager from './gradebook2-manager';

/**
 * Main Gradebook Page
 * Route: /dashboard/academics/gradebook
 * Displays the primary management dashboard for student grades, class rankings,
 * and academic reporting.
 */
export default function GradebookPage() {
    return <GradebookManager />;
}
